#include <bits/stdc++.h>
using namespace std;

int soma(int n) {
    int resultado = 0; // Apenas uma variável inteira
    for(int i = 0; i < n; i++) {
        resultado += i;
    }
    return resultado;
}